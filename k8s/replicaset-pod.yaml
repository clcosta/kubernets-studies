apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: madr
  labels:
    app: madr
spec:
  selector:
    matchLabels:
      app: madr
  replicas: 2
  template:
    metadata:
      labels:
        app: madr
    spec:
      containers:
        - name: madr
          image: clcostaf/madr:latest